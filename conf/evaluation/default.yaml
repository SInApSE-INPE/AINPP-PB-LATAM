# ==============================================================================
# Benchmark Evaluation Protocol
# ==============================================================================
region: ainpp-amazon-basin
checkpoint: "" # Passado via CLI usualmente

# Thresholds in mm/h that dictate event occurrence
thresholds_mm_h: [0.1, 1.0, 5.0, 10.0]

# Pre-defined observation times (Used loosely if dataset dictates it)
lead_times_min: [10, 20, 30, 40, 50, 60]

# --- Modular Evaluation Branches ---
categorical: true
continuous: true
probabilistic: true
object_based: true
sharpness: true
consistency: true

max_batches: null
